[project]
name = "MidiTest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mido>=1.3.0",
    "python-rtmidi>=1.5.8",  # Backend essentiel pour mido
    "pyinstaller>=6.20.0",
]
